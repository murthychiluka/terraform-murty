# main.tf
# VPC lookup removed. The root module passes vpc_id and subnet_ids directly.

# No data sources are required for this module because the root module forwards the VPC ID and subnet IDs directly.
